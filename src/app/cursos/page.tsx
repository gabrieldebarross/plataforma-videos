import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Codarse - Cursos",
    description: "Cursos disponíveis no Codarse."
}

export default function PageCursos(){
    return (
        <main>
            Cursos
        </main>
    )
}